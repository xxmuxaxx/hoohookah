# hoohookah

Лендинг с конструктором кальяна. Vite + SCSS + JS без фреймворков.

Нужен Node.js 20.19+.

```sh
npm install
npm start       # dev-сервер
npm run build   # сборка в public/
npm run lint    # eslint + stylelint с автоисправлением
```
